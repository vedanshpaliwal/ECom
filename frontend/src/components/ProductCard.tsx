import {addToCart} from '../store';
export default function ProductCard({product,onCart}:{product:any;onCart:()=>void}){const image=product.images?.find((x:any)=>x.isPrimary)?.url??product.images?.[0]?.url;return <article className="productCard"><div className="productImage">{image?<img src={image} alt={product.name}/>:<div className="placeholder">Handmade</div>}</div><div className="productInfo"><span>{product.category?.name}</span><h3>{product.name}</h3><p>{product.description}</p><div className="price">₹{Number(product.price).toLocaleString('en-IN')}</div><button className="primary" onClick={()=>{addToCart({productId:product.id,name:product.name,price:Number(product.price),image,quantity:1});onCart()}}>Add to cart</button></div></article>}
